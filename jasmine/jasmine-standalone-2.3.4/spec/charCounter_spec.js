describe('charCounter', function () {

  it('return the number of times a given char happens in a string', function () {
    expect(nthRoot('me', 'i')).toEqual(0);
    expect(nthRoot('My Company', 'm')).toEqual(2);

  });

});