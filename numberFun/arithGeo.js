// Determine if an array of numbers follows a
// geometric or arithmetic pattern
// Geometric pattern, where a given number 
// will be divisible by a previous number
// or an arithmetic pattern, 
//where each element increases by a constant amount.

function arithGeo(array) {
  //check if it's actually an array
  //check length, must be greater than 1
  //determine if it's a geo or arith pattern by comparing #'s



  if() {
  //arith
  } 
  else if() {
  //geometric
  }
  else if {
  //both
  } else {
    //neither
    return -1;
  }


}

